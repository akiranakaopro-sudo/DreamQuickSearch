<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/search_home_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <!-- Baked wallpaper blur (Dialer InCall blurView), set in SearchHomeBackdrop. -->
    <ImageView
        android:id="@+id/blur_backdrop"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@null"
        android:importantForAccessibility="no"
        android:scaleType="centerCrop" />

    <!-- Live Oplus frost host when wallpaper pixels cannot be read. -->
    <View
        android:id="@+id/blur_layer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:importantForAccessibility="no"
        android:visibility="gone" />

    <!-- Optional scrim slot (kept hidden — frost shows without a black veil). -->
    <View
        android:id="@+id/blur_scrim"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:importantForAccessibility="no"
        android:visibility="gone" />

    <LinearLayout
        android:id="@+id/search_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <include
            android:id="@+id/search_bar"
            layout="@layout/view_search_bar"
            android:layout_width="match_parent"
            android:layout_height="@dimen/search_bar_height"
            android:layout_marginHorizontal="@dimen/search_bar_margin_horizontal" />

        <FrameLayout
            android:id="@+id/results_pane"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginTop="@dimen/search_bar_results_gap"
            android:layout_weight="1">

            <gd.app.quicksearch.ui.home.SearchResultsRecyclerView
                android:id="@+id/search_results"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                android:clickable="true"
                android:fadeScrollbars="true"
                android:focusable="true"
                android:overScrollMode="always"
                android:scrollbars="none"
                app:couiRecyclerViewEnableVibrator="true" />

            <com.coui.appcompat.emptyview.COUIEmptyStateView
                android:id="@+id/empty_state"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:visibility="gone"
                app:anim_autoPlay="true"
                app:anim_fileName="no_search_results_dark.json"
                app:emptyViewSizeType="medium"
                app:titleText="@string/search_no_results" />

            <TextView
                android:id="@+id/exit_hint"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom|center_horizontal"
                android:layout_marginBottom="@dimen/search_exit_hint_margin_bottom"
                android:paddingHorizontal="16dp"
                android:paddingVertical="8dp"
                android:text="@string/search_swipe_up_to_exit"
                android:textColor="@color/search_exit_hint"
                android:textSize="@dimen/search_exit_hint_text_size"
                android:visibility="visible" />
        </FrameLayout>
    </LinearLayout>

</FrameLayout>
