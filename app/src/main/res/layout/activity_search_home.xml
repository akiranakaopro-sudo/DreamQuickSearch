<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/search_home_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <ImageView
        android:id="@+id/blur_backdrop"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@null"
        android:importantForAccessibility="no"
        android:scaleType="centerCrop" />

    <View
        android:id="@+id/blur_layer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:importantForAccessibility="no" />

    <View
        android:id="@+id/blur_scrim"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/search_home_scrim"
        android:importantForAccessibility="no" />

    <com.coui.appcompat.searchview.COUISearchBar
        android:id="@+id/search_bar"
        style="@style/Widget.COUI.COUISearchBar.Dark"
        android:layout_width="match_parent"
        android:layout_height="@dimen/coui_search_bar_height"
        android:layout_gravity="top"
        android:layout_marginHorizontal="16dp"
        app:functionalButtonText="@string/search_action"
        app:searchHint="@string/search_hint" />

</FrameLayout>
